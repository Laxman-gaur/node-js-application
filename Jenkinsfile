pipeline {
    agent any

    stages {
        stage('git-clone ') {
            steps {
                sh 'git clone https://github.com/Laxman-gaur/node-js-application.git' 
            }
        }
    }
}
